files:{
  com.js -communication between processes
    send (data) {
      sends data to worker window
    }

  main.js -main file

  requires.js -contains require node modules

  ui.js -file -modifying user interface
    
}
